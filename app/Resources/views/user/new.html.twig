{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-success" href="{{ path('user_index') }}"><span class="glyphicon glyphicon-list"></span> {{ 'back to the list'|trans }}</a>
    <a class="btn btn-default" href="{{ path('user_index') }}"><span class="glyphicon glyphicon-remove"></span> {{ 'Cancel'|trans }}</a>
    
    <h1>{{ 'User creation'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-10"><button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-plus"></span> {{ 'create'|trans }}</button></div>
        </div>
        
    {{ form_end(form) }}

    
    
{% endblock %}

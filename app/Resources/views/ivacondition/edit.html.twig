{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-success" href="{{ path('ivacondition_index') }}"><span class="glyphicon glyphicon-list"></span> {{ 'back to the list'|trans }}</a>
    
    <h1>{{ 'Ivacondition edit'|trans }}</h1>

    <br>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-10"><button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-edit"></span> {{ 'edit'|trans }}</button></div>
            
        </div>
    {{ form_end(edit_form) }}

    
{% endblock %}
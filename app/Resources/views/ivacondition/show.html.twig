{% extends 'base.html.twig' %}

{% block body %}
    <h1>IvaCondition</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ivaCondition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ivaCondition.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ivacondition_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ivacondition_edit', { 'id': ivaCondition.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
